import { ConnectPlugin, ComponentConfig, ComponentData, PluginContext } from "@zeplin/cli";
import { Story } from "./storybook/stories";
interface StorybookPluginConfig {
    url?: string;
    fetchStories?: boolean;
    targetUrl?: string;
    startScript?: string;
    command?: string;
    format?: "old" | "new";
    useDocsPage?: boolean;
    failFastOnErrors?: boolean;
    ignoreSSLErrors?: boolean;
}
export default class implements ConnectPlugin {
    stories: Story[];
    targetUrl: string;
    sourceUrl: string;
    config: StorybookPluginConfig;
    useDocsPage?: boolean;
    init(pluginContext: PluginContext): Promise<void>;
    process(componentConfig: ComponentConfig): Promise<ComponentData>;
    supports(x: ComponentConfig): boolean;
    private storiesLoaded;
    private createLink;
}
export {};
