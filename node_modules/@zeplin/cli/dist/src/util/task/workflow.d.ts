import { TaskContext } from "./types";
import { Task } from "./task";
export declare class Workflow<T = TaskContext> {
    private readonly context;
    private readonly tasks;
    constructor(params: {
        context: T;
        tasks: Task<TaskContext>[];
    });
    run(): Promise<T>;
}
