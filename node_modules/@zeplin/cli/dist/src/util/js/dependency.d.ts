import { PackageJson } from "./config";
export interface JsDependencies {
    packageJson?: PackageJson | null;
    bowerJson?: PackageJson | null;
}
export declare function getDependencyVersion(packageJson: PackageJson, name: string): string | undefined;
export declare function hasDependency(packageJson: PackageJson, name: string): boolean;
