import { GitConfig } from "../commands/connect/interfaces/config";
export declare function detectGit(params?: {
    path?: string;
}): Promise<{
    type: "bitbucket" | "github" | "gitlab";
    config: GitConfig;
} | null>;
