import { ZeplinApi } from "../../api";
import { AuthenticationService } from "../../service/auth";
import { ConnectedBarrelComponents, ConnectedBarrels } from "./interfaces/api";
export declare class ConnectedComponentsService {
    zeplinApi: ZeplinApi;
    authService: AuthenticationService;
    constructor({ zeplinApi, authService }?: {
        zeplinApi?: ZeplinApi;
        authService?: AuthenticationService;
    });
    uploadConnectedBarrels(connectedBarrelComponents: ConnectedBarrelComponents[]): Promise<void>;
    deleteConnectedBarrels(connectedComponents: ConnectedBarrels[]): Promise<void>;
    private upload;
    private delete;
}
