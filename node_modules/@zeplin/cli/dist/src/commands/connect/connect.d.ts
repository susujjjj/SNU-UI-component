import { ConnectedBarrelComponents } from "./interfaces/api";
declare const generateConnectedComponents: (options: Partial<Pick<ConnectOptions, "configFiles" | "plugins">>) => Promise<ConnectedBarrelComponents[]>;
declare function connect(options: ConnectOptions): Promise<void>;
export interface ConnectOptions {
    configFiles: string[];
    devMode: boolean;
    devModePort: number;
    devModeWatch: boolean;
    plugins: string[];
}
export { generateConnectedComponents, connect };
