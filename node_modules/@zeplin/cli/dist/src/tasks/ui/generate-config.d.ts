import { TaskUI } from "../../util/task";
import { InitializeContext } from "../context/initialize";
export declare const initial: () => TaskUI;
export declare const completed: (ctx: InitializeContext) => TaskUI;
