"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.detectEnvironment = void 0;
const task_1 = require("../util/task");
const ui = __importStar(require("./ui/detect-environment"));
const config_1 = require("../util/js/config");
const config_2 = require("../commands/connect/config");
const logger_1 = __importDefault(require("../util/logger"));
const text_1 = require("../util/text");
const detect = (ctx) => __awaiter(void 0, void 0, void 0, function* () {
    const packageJson = yield config_1.getPackageJson();
    const installedGlobally = !packageJson;
    const [configFile] = yield config_2.getComponentConfigFiles([ctx.cliOptions.configFile]);
    const installedPlugins = (configFile.plugins || []).map(p => p.name);
    logger_1.default.debug(`Environment: ${text_1.stringify({
        installedPlugins,
        installedGlobally
    })}`);
    ctx.installedPlugins = installedPlugins;
    ctx.installGlobally = installedGlobally;
    ctx.packageJson = packageJson;
});
exports.detectEnvironment = new task_1.Task({
    steps: [
        detect,
        task_1.transitionTo(ui.completed)
    ],
    initial: ui.initial
});
